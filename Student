public class Student {
    private String name;
    private int id;

    Student(String n, int id) {
        this.name = n;
        this.id = id;
    }

    public int getId() {
        return id;
    }
}
